<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HomeJamesWIL" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/homeJames</item>
        <item name="colorPrimaryVariant">@color/homeJames</item>
        <item name="colorOnPrimary">@color/homeJames</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/homeJames</item>
        <item name="colorSecondaryVariant">@color/homeJames</item>
        <item name="colorOnSecondary">@color/homeJames</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:textAppearance">@color/black</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="circleImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="AppTheme.Picker" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="LoginTextInputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/white</item>
        <item name="boxStrokeWidth">2dp</item>
    </style>





</resources>